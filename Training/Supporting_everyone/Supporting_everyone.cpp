#include <bits/stdc++.h>

using namespace std;

#define int long long

signed main() {
    int N;
    int M;

    cin >> N >> M;

    vector<int> k(N);
    vector<vector<int>> c(N);
    for(int i = 0; i < N; i++) {
        cin >> k[i];

        vector<int> colors(k);
        for(int j = 0; j < k[i]; j++) {
                       
        }
    }
}